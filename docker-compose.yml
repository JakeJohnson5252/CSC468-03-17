services:
  go_app:
    build:
      context: ./docker/
      tags:
        - "jakejohnson52/go_server:latest"
    container_name: go_app
    image: jakejohnson52/go_server:latest
    restart: always
    expose:
      - 8080
    ports:
      - 8080:8080
